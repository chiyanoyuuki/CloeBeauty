import { ScrollAppearDirective } from './scroll-appear.directive';

describe('ScrollAppearDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAppearDirective();
    expect(directive).toBeTruthy();
  });
});
